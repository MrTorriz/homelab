.env
.env.local
*.key
*.pem
*.crt
secrets/
appdata/
backups/

.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Lokal kontextfil för Claude Code — ska aldrig pushas
CLAUDE.md

# Lokal arbetsjournal (skrivs av /homelab-skill) — ska aldrig pushas
JOURNAL.md

# Lokal referensfil — skills/plugins vi använder i sessionen, ska aldrig pushas
TOOLS.md

# Playwright MCP debug artifacts (console logs, page snapshots)
.playwright-mcp/

# Personal sanitisation patterns — read by scripts/sanitize-check.sh
# .sanitize-patterns.example is committed as a template
.sanitize-patterns
